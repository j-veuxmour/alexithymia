# ALEXITHYMIA — Environment Variables
# Copy to .env and fill in actual values. Never commit .env.

# Runtime
NODE_ENV=development
LOG_LEVEL=info

# TODO: populate as packages are added
# - Postgres (DATABASE_URL)
# - Redis (REDIS_URL)
# - Solana RPC (RPC_URL, HELIUS_API_KEY)
# - LLM providers (ANTHROPIC_API_KEY, OPENROUTER_API_KEY)
# - LangFuse (LANGFUSE_HOST, LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY)
# - Wallet keystore (WALLET_KEYSTORE_PATH, WALLET_KEYSTORE_PASSPHRASE)
